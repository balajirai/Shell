#!/bin/bash

mkdir -p dir1       # This will create a directory. Also if directory already exits then, just move on (no error, no override)
# mkdir dir1          # This will create a directory. If directory already exits then it throws error that it already exits